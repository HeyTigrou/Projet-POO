#include <iostream>
#include <fstream>
#include <string>

using namespace std;
/*
int main() {
	fstream myFile;

	myFile.open("test.txt", ios::out);
	if (myFile.is_open()) {
		myFile << "Hello\n";
		myFile << "This is seconde line\n";
		myFile.close();
	}

	myFile.open("test.text", ios::app);
	if (myFile.is_open()) {
		myFile << "Hello2\n";
		myFile.close();
	}

	system("PAUSE");

	myFile.open("test.txt", ios::in);
	if (myFile.is_open()){
		string line;
		while (getline(myFile, line)) {
			cout << line << endl;
		}
		myFile.close();
	}
}
*/

/*
class Vehicule {
public :
	string brand = "Ford";
	void honk() {
		cout << "Tut tut\n";
	}
};

class Car : public Vehicule {
public :
	string model;
	Car(string m) {
		this->model = m;
	}
};

int main() {
	Car myCar1("Escort");
	Car myCar2("Mustang");
	myCar1.honk();
	cout << myCar1.brand + " " + myCar1.model<<endl;
	cout << myCar2.brand + " " + myCar2.model<<endl;
}
*/

